import pytest

class Test_assing():


    def func_to_check_ascening_order(self,new_list):


        output_list = sorted(new_list)
        if new_list == output_list:
            return True
        else:
            return False

    # pfunc_to_check_ascening_order(list))


    def test_function_with_pass(self):
        nums = "1,2,3"

        self.func_to_check_ascening_order(list) == True

    def test_function_with_fail(self):
        nums = "1,2,3"

        self.func_to_check_ascening_order(num) == True

{"name":"", "id":"1"}


configfiles = [""]
